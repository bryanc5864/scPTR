# Pinned dependency versions used to produce the results in the paper.
# Tested with Python 3.10 and 3.11 on Windows 11 and Linux.
#
# Install:
#   pip install -r requirements.txt
#   pip install -e .          # optional: install scptr as a package
#
# Notes:
#   - torch is required only for the DeepPTR generative model
#     (scripts/deep/* and Section 3.3 of the paper). The analytic
#     estimator and all Section 4.1-4.4 results work without it.
#   - pooch handles the on-the-fly download of the public datasets
#     listed in data/README.md.

# --- Core single-cell + numeric stack ---
anndata>=0.8,<1.0
scanpy>=1.9,<2.0
numpy>=1.21,<2.2
scipy>=1.7,<1.16
numba>=0.55,<0.66
pandas>=1.3,<2.4
matplotlib>=3.5,<3.10
seaborn>=0.11,<0.14

# --- Datasets (Pooch fetcher) ---
pooch>=1.6

# --- Statistics / clustering used by analysis scripts ---
scikit-learn>=1.0
statsmodels>=0.13
leidenalg>=0.9
igraph>=0.10
pynndescent>=0.5
umap-learn>=0.5

# --- DeepPTR (optional) ---
torch>=2.0

# --- Test runner (optional, only needed for tests/) ---
pytest>=7.0
